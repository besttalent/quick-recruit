<%= render "shared/navbar/base",
    brand_href: nil,
    show_new_candidate_button: false,
    nav_links: [
      { label: current_user.first_name, href: member_timeline_path(current_user), active: current_page?(member_timeline_path(current_user)) },
      { label: "Interviews", href: interviews_path, active: current_page?(interviews_path) }
    ],
    primary_menu_links: [
      { label: "Personal Settings", href: "/settings/profile" }
    ] %>
