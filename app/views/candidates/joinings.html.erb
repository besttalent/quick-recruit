<section class="page-bg-shell">
  <div class="page-container-wide">
    <div class="page-card">
      <div class=" divide-y dark:divide-gray-700">
        <div class="page-panel-header-row">
          <div>
            <h1 class="heading-lg-soft">Upcoming Joinings</h1>
            <p class="text-muted-sm">Following <span class="font-bold"><%= @candidates.count %></span> candidates are about to join us.</p>
          </div>
          <a type="button" href="/candidates/new"
                class="btn-primary">
            Add new candidate
          </a>
        </div>
      </div>
      <div class="sidebar-scroll-x">
        <table class="input-muted">
          <thead class="table-head-muted">
            <tr>
              <th scope="col" class="px-4 py-3">Name</th>
              <th scope="col" class="px-4 py-3">Phone</th>
              <th scope="col" class="px-4 py-3">Job Association</th>
              <th scope="col" class="table-head-cell">Owner</th>
              <th scope="col" class="table-head-cell">Joining On</th>
              <th scope="col" class="table-head-cell">Joining time</th>
            </tr>
          </thead>
          <tbody>
            <%= render partial: "joining", collection: @candidates, as: :candidate %>
          </tbody>
        </table>
      </div>
      <div class="sidebar-actions-row">
        <%== render partial: "shared/paginator", locals: { pagy: @pagy } %>
      </div>
    </div>
  </div>
</section>
