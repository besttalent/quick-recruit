<section class="page-bg-shell">
  <div class="page-container-wide">
    <div class="page-card">
      <div class=" divide-y dark:divide-gray-700">
        <div class="panel-header-row">
          <div>
            <h1 class="heading-lg-soft">Alumni</h1>
            <p class="text-muted-sm">Following <span class="font-bold"><%= @candidates.count %></span> candidates were either offered or they joined us. These are the best candidates we got from any source.</p>
          </div>
          <a type="button" href="/candidates/new"
                class="btn-primary">
            Add new candidate
          </a>
        </div>
      </div>
      <div class="scroll-x">
        <table class="input-muted">
          <thead class="table-head-muted">
            <tr>
              <th scope="col" class="px-4 py-3">Name</th>
              <th scope="col" class="px-4 py-3">Phone</th>
              <th scope="col" class="px-4 py-3">Job Association</th>
              <th scope="col" class="table-head-cell">Owner</th>
              <th scope="col" class="table-head-cell">Updated On</th>
              <th scope="col" class="table-head-cell">Last known Status</th>
            </tr>
          </thead>
          <tbody>
            <%= render partial: "alumni", collection: @candidates, as: :candidate %>
          </tbody>
        </table>
      </div>
      <div class="actions-row">
        <%== render partial: "shared/paginator", locals: { pagy: @pagy } %>
      </div>
    </div>
  </div>
</section>
