<li class="timeline-item">
  <span class="timeline-node"><%= display_xsmall_avatar(event.user) %></span>
  <div class="toolbar-compact">
    <time class="timeline-meta-muted"><%= display_created_at_with_time(event) %></time>
    <div class="body-muted"><a href="<%= member_timeline_path(event.user) %>" class="link-strong-underline"><%= event.user.name %></a> assigned candidate to recruiter <%= event.action_for_context %></div>
  </div>
</li>
