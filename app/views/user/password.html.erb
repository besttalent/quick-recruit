<main class="settings-shell">
  <div class="settings-grid">
    <%= render "shared/settings_menu" %>
    <main class="settings-content">
      <%= turbo_frame_tag "password_form" do %>
        <%= render "user/forms/password", user: @user %>
      <% end %>
    </main>
  </div>
</main>
