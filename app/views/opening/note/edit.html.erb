<div class="py-8 px-4 mx-auto max-w-4xl lg:py-8">
  <h1 class="heading-lg-soft">Edit <%= @opening.title %> Intenal Note</h1>
  <p class="text-muted-sm">Edit intenal note about this job description.</p>
  <%= form_with model: @opening, url: opening_note_path(@opening), method: :patch do |form| %>
    <div class="field mt-8">
      <%= form.rich_text_area :note %>
      <div class="flex items-center space-x-2">
        <button type="submit" class="btn-primary mt-4">
          Edit Intenal Note
        </button>
        <a type="button" href="<%= opening_note_index_path(@opening) %>"
                class="btn-secondary mt-4">
          Cancel
        </a>
      </div>
    </div>
  <% end %>
</div>
